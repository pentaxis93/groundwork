{
  "scope": "Input validation for the data ingestion pipeline to reject malformed records before processing",
  "functional_requirements": [
    "Validate record schema before ingestion",
    "Reject records missing required fields",
    "Log rejected records with reason codes"
  ],
  "non_functional_requirements": [
    "Validation must add less than 50ms latency per record"
  ],
  "constraints": [
    "Must not break existing valid record formats"
  ]
}
