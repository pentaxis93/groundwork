{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/pentaxis93/groundwork/main/schemas/research-record.schema.json",
  "title": "Research Record",
  "description": "Validates the content of a research-record artifact. Naming convention: research-record-{topic-slug}.yaml where topic-slug matches the topic field.",
  "type": "object",
  "required": ["topic", "findings", "sources", "date"],
  "additionalProperties": false,
  "properties": {
    "topic": {
      "type": "string",
      "description": "Kebab-case slug identifying the research topic.",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
    },
    "findings": {
      "type": "array",
      "description": "Key findings from the research.",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "sources": {
      "type": "array",
      "description": "Sources consulted during research.",
      "items": {
        "$ref": "#/$defs/source"
      }
    },
    "date": {
      "type": "string",
      "description": "Date of research in YYYY-MM-DD format.",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    }
  },
  "$defs": {
    "source": {
      "type": "object",
      "description": "A source consulted during research.",
      "required": ["url"],
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string",
          "description": "URL of the source.",
          "minLength": 1
        },
        "title": {
          "type": "string",
          "description": "Optional title of the source."
        }
      }
    }
  }
}
