{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/pentaxis93/groundwork/main/schemas/documentation-record.schema.json",
  "title": "Documentation Record",
  "description": "Validates the content of a documentation-record artifact. Records which docs were updated, which were verified accurate, and which were deferred to tracking work-units.",
  "type": "object",
  "required": ["work_unit", "updated_docs", "verified_accurate_docs", "tracking_work_units"],
  "additionalProperties": false,
  "properties": {
    "work_unit": {
      "type": "string",
      "description": "Common envelope — threads to work unit.",
      "pattern": "^[a-z][a-z0-9]*([-.][a-z0-9]+)*$"
    },
    "updated_docs": {
      "type": "array",
      "description": "Documentation files updated in this change.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "verified_accurate_docs": {
      "type": "array",
      "description": "Documentation reviewed and confirmed accurate without edits.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "tracking_work_units": {
      "type": "array",
      "description": "Work-units filed for documentation follow-up.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    }
  }
}
